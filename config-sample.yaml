log:
  level: info
  formatter: text
  file:
    filename: elvmcp.log
    maxsize: 100
    maxbackups: 5
  # named:
  #   /auth:
  #     level: debug
  #   /mcpserver:
  #     level: debug

server:
  oauth_issuer: ""   # required: OAuth2 issuer URL (e.g. https://<your-auth-provider>)
  resource_url: ""   # required: this server's public URL (e.g. https://<your-ngrok-url>)

fabric:
  search_base_url: ""
  image_base_url: ""
  vid_base_url: ""
  eth_url: ""
  qspace_id: ""
  api_url: "" #optional, defaults to https://main.net955305.contentfabric.io
  tag_store_url: "" #optional, defaults to https://ai.contentfabric.io/tagstore
  ai_tagger_url: "" #optional, defaults to https://ai.contentfabric.io/tagging-live 
  

tenants:
  - id: studio-a
    users:
      - "user|abc123"   # OAuth2 sub claim (find it by decoding the user's JWT)
      - "user|def456"
    fabric:
      private_key: "0x..."
      qlibid_index: "ilib..."
      qid_index: "iq__..."
      search_collection_id: "iq__..."

  - id: studio-b
    users:
      - "user|xyz789"
    fabric:
      private_key: "0x..."
      qlibid_index: "ilib..."
      qid_index: "iq__..."
      search_collection_id: "iq__..."
